export const metadata = {
  title: "GSN Admin Dashboard",
  description: "Internal Garda Samudra Nusantara lead management dashboard for authorized administrators."
};

export default function AdminLayout({ children }) {
  return children;
}
